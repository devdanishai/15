mem0 setup

```bash
python -m venv venv

source venv/bin/activate

pip install mem0ai

# create project 006-mem0
# run test.py

from mem0 import Memory

config = {
    "llm": {
        "provider": "ollama",
        "config": {
            "model": "qwen3:32b",
            "ollama_base_url": "http://localhost:11434",
        }
    },

    "embedder": {
        "provider": "ollama",
        "config": {
            "model": "nomic-embed-text:latest",
            "ollama_base_url": "http://localhost:11434",
        }
    }
}

memory = Memory.from_config(config)

memory.add(
    "My name is Danish and I work on AI systems.",
    user_id="user1"
)

result = memory.search(
    "What does the user work on?",
    user_id="user1"
)

print(result)
```

observation:

it has error
