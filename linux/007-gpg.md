# Create GPG encrypted files 
```bash
gpg -c file.txt
# or
gpg -c file.txt && shred -u file.txt

```
- it ask for passphrase
- passphrase = 12345678

### decrypt file
```bash
gpg -o file.txt -d file.txt.gpg
```